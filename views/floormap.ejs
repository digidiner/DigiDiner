<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Restaurant Floor Map</title>
    <link rel="stylesheet" href="css/bootstrap.min.css">
    <style>
        .table-cell {
            width: 100px;
            height: 100px;
            border: 1px solid #ccc;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
        }
    </style>
</head>

<body>
    <header>
        <div class="px-3 py-4 text-bg-dark border-bottom">
            <div class="container-fluid justify-content-lg-start">
                <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
                    <a href="/"
                        class="d-flex align-items-center my-2 my-lg-0 me-lg-auto text-white text-decoration-none">
                        <img src="images/logoCloseup.png" role="img" alt="LogoClose" width="100" height="84">
                    </a>
                    <ul class="nav col-12 col-lg-auto my-2 justify-content-center my-md-0 text-small">
                        <li>
                            <a href="/" class="nav-link text-secondary">
                                <svg class="bi bi-house-door" width="24" height="24">
                                    <use xlink:href="#house-door" />
                                </svg>
                                Home
                            </a>
                        </li>
                        <li>
                            <a href="/clock" class="nav-link text-white">
                                <svg class="bi bi-clock-history" width="24" height="24">
                                    <use xlink:href="#clock-history" />
                                </svg>
                                Clock
                            </a>
                        </li>
                        <li>
                            <a href="/orders" class="nav-link text-white">
                                <svg class="bi bi-list-ul" width="24" height="24">
                                    <use xlink:href="#list-ul" />
                                </svg>
                                Orders
                            </a>
                        </li>
                        <li>
                            <a href="/inventory" class="nav-link text-white">
                                <svg class="bi bi-file-earmark-spreadsheet" width="24" height="24">
                                    <use xlink:href="#file-earmark-spreadsheet" />
                                </svg>
                                Inventory
                            </a>
                        </li>
                        <li>
                            <a href="floormap" class="nav-link text-white">
                                <svg class="bi bi-map" width="24" height="24">
                                    <use xlink:href="#map" />
                                </svg>
                                Floor Map
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </header>
    <div class="container">
        <h1 class="mt-4 mb-4">Restaurant Floor Map</h1>

        <div class="row">
            <div class="col-md-8">
                <div class="table-grid">
                    <% var rowCount=Math.ceil(tables.length / 3); %>
                        <% for (var i=0; i < rowCount; i++) { %>
                            <div class="row">
                                <% var startIndex=i * 3; %>
                                    <% var endIndex=Math.min(startIndex + 3, tables.length); %>
                                        <% for (var j=startIndex; j < endIndex; j++) { %>
                                            <div class="col-md-4">
                                                <div class="table-cell" data-table-id="<%= tables[j].id %>"
                                                    data-status="<%= tables[j].status %>">
                                                    Table <%= tables[j].id %>
                                                </div>
                                            </div>
                                            <% } %>
                            </div>
                            <% } %>
                </div>
            </div>
        </div>


        <div class="col-md-4">
            <h4>Selected Table: <span id="selectedTable"></span></h4>
            <h5>Status: <span id="selectedTableStatus"></span></h5>
            <button class="btn btn-primary" data-toggle="modal" data-target="#updateStatusModal">Update
                Status</button>
        </div>
    </div>
    </div>

    <!-- Order Modal -->
    <div class="modal fade" id="orderModal" tabindex="-1" role="dialog" aria-labelledby="orderModalTitle"
        aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="orderModalTitle">Order for Table <span id="orderTableId"></span></h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <!-- Place your order content here -->
                    <p>This is the order details for the selected table.</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Menu Modal -->
    <div class="modal fade" id="menuModal" tabindex="-1" role="dialog" aria-labelledby="menuModalTitle"
        aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="menuModalTitle">Menu for Table <span id="menuTableId"></span></h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <!-- Place your menu content here -->
                    <p>This is the menu for the selected table.</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Update Status Modal -->
    <div class="modal fade" id="updateStatusModal" tabindex="-1" role="dialog" aria-labelledby="updateStatusModalTitle"
        aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="updateStatusModalTitle">Update Table Status</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div class="form-group">
                        <label for="statusSelect">Select Status:</label>
                        <select class="form-control" id="statusSelect">
                            <option value="unoccupied">Unoccupied</option>
                            <option value="occupied">Occupied</option>
                            <option value="dirty">Dirty</option>
                        </select>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                    <button type="button" class="btn btn-primary" onclick="updateTableStatus()">Update</button>
                </div>
            </div>
        </div>
    </div>

    <script src="/js/jquery.min.js"></script>
    <script src="/js/bootstrap.min.js"></script>
    <script>
        $(document).ready(function () {
            $('.table-cell').on('click', function () {
                const tableId = $(this).data('table-id');
                const status = $(this).data('status');

                // Update the selected table information
                $('#selectedTable').text(tableId);
                $('#selectedTableStatus').text(status);

                // Show the corresponding modal based on the table status
                if (status === 'occupied') {
                    $('#orderTableId').text(tableId);
                    $('#orderModal').modal('show');
                } else if (status === 'unoccupied') {
                    $('#menuTableId').text(tableId);
                    $('#menuModal').modal('show');
                } else if (status === 'dirty') {
                    // Show the most recent table's final bill (placeholder)
                    alert(`Showing final bill for Table ${tableId}`);
                }
            });
        });

        function updateTableStatus() {
            const selectedTable = $('#selectedTable').text();
            const status = $('#statusSelect').val();

            // Update the table status
            $(`.table-cell[data-table-id="${selectedTable}"]`).data('status', status);
            $(`.table-cell[data-table-id="${selectedTable}"]`).removeClass('table-occupied table-unoccupied table-dirty');

            // Add the corresponding class based on the updated status
            if (status === 'occupied') {
                $(`.table-cell[data-table-id="${selectedTable}"]`).addClass('table-occupied');
            } else if (status === 'unoccupied') {
                $(`.table-cell[data-table-id="${selectedTable}"]`).addClass('table-unoccupied');
            } else if (status === 'dirty') {
                $(`.table-cell[data-table-id="${selectedTable}"]`).addClass('table-dirty');
            }

            // Update the selected table status
            $('#selectedTableStatus').text(status);

            // Close the update status modal
            $('#updateStatusModal').modal('hide');
        }
    </script>
    <%- include('partials/footer') %>
</body>

</html>